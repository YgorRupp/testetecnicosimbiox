import styled from "styled-components";

export const ContainerSearchBar = styled.nav`
  div {
    position: relative;
  }

  button {
    position: absolute;
    top: 8px;
    right: 10px;

    background: none;
    border: none;
  }
`;
